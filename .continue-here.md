---
project: claude-ipc (collab tool)
instance: claude-ipc
last_updated: 2026-03-31T20:45:50Z
status: active
---

<current_state>
All session work committed and pushed to main (HEAD: de72fb1). Server on kali needs
one more restart to pick up the latest binary (SSE web dashboard + /metrics endpoint).
</current_state>

<completed_work>

- Local .collab.toml support: walk CWD upward, local overrides global. Each worker dir
  in /Users/operator/code/d4builder/workers/ now has its own .collab.toml.
- Direction arrows: ← / → on all message output (list, status, watch, stream, add, broadcast).
- Web dashboard token auth: password field, persisted to localStorage, sent as Bearer header.
- SSE streaming: `collab stream` command — GET /events/:instance_id, instant push, zero polling,
  exponential backoff reconnect (1→30s). Worker CLI identity: @claude-ipc.
- GET /metrics endpoint: messages_total, messages_last_hour, active_workers, sse_subscribers, uptime_secs.
- Server reads token from ~/.collab.toml as fallback (no more --token flag required).
- Web app SSE: GET /events (all messages, no filter) for dashboard. Auth via ?token= query param
  (EventSource can't set headers). Roster poll slowed from 3s to 30s. ~200 req/min → ~2 req/min.
- All commits pushed to github.com:jabberwock/claude-ipc.git

</completed_work>

<remaining_work>

- Kali needs: `git pull && ./build.sh` then restart collab-server — picks up token from ~/.collab.toml
  automatically now, no flags needed.
- /metrics returning 404 until kali server is restarted with new binary.
- @openrouter waiting on /metrics to build token usage report for @jabberwock.
- Cursor IDE support: add .cursorrules template to `collab init`, README section. @jabberwock asked.
- Lazy-loading for web UI buffer — waiting on @project-manager to clarify which buffer
  (emoji picker? message feed? builds list?). Replied asking.

</remaining_work>

<decisions_made>

- SSE over polling: @openrouter proposed, implemented with tokio::sync::broadcast channel.
  browser EventSource uses ?token= query param since it can't set headers.
- Local config hierarchy mirrors git: local .collab.toml > ~/.collab.toml. Same pattern
  as collab CLI already used for instance/recipients.
- /events (no filter) for web dashboard — streams all messages. Per-instance /events/:id
  for CLI workers.
- collab watch kept as-is for backwards compat; collab stream is new preferred command.

</decisions_made>

<blockers>

- Kali server restart pending (human action) — blocks /metrics and SSE web dashboard going live.

</blockers>

<context>
Working as @claude-ipc on the collab tool itself. Token in ~/.collab.toml on this machine.
Cron job bd5dccd1 runs `collab list` every minute (session-only, dies when Claude exits).
Background stream running at task ID b21by9hzq but only delivers when actively in conversation.
d4builder workers are separate — their collab work is separate from this tool work.
</context>

<next_action>
Run `collab list` to check for replies from @openrouter, @project-manager, @jabberwock.
Then follow up on kali server restart status.
</next_action>
